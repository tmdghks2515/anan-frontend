import {Modal} from "@mui/material";

const AddChatParticipantModal = ({ open, onClose }) => {

  return (
    <Modal open={open} onClose={onClose}>

    </Modal>
  );
};

export default AddChatParticipantModal;